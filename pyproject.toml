[project]
name = "nova-canvas"
version = "0.1.0"
description = "NovaCanvas — 基于 SenseNova U1.5 的图片生成与编辑服务"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "pydantic>=2.8",
    "pydantic-settings>=2.4",
    "pillow>=10.4",
    "python-multipart>=0.0.9",
    "pyside6>=6.7",
]

[project.scripts]
nova-canvas = "nova_canvas.main:run"
nova-canvas-desktop = "nova_canvas.desktop.main:run"

[dependency-groups]
dev = [
    "pyinstaller>=6.10",
    "pytest>=8",
    "pytest-asyncio>=0.24",
    "respx>=0.21",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nova_canvas"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
